<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.t_mobop_werewolf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlayingHostActivity"></activity>
        <activity android:name=".CreatingRoomActivity" />
        <activity android:name=".JoiningRoomActivity" />
        <activity android:name=".Frag_Actions_Werewolf" />
        <activity android:name=".Frag_Actions_Villager" />
        <activity android:name=".WaitingRoomActivity" />
        <activity android:name=".PlayingActivity" />
        <activity android:name=".SplashScreen"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>
